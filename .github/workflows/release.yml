name: Release

on:
  push:
    tags:
      - "*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Build with tag version
        env:
          VERSION: ${{ github.ref_name }}
        run: |
          make build VERSION="${VERSION}"

      - name: Package artifacts
        env:
          VERSION: ${{ github.ref_name }}
        run: |
          mkdir -p dist
          cp paraspeech dist/paraspeech-linux-amd64
          tar -C dist -czf "dist/paraspeech-${VERSION}-linux-amd64.tar.gz" paraspeech-linux-amd64
          sha256sum "dist/paraspeech-${VERSION}-linux-amd64.tar.gz" > "dist/paraspeech-${VERSION}-linux-amd64.tar.gz.sha256"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          generate_release_notes: true
          files: |
            dist/paraspeech-${{ github.ref_name }}-linux-amd64.tar.gz
            dist/paraspeech-${{ github.ref_name }}-linux-amd64.tar.gz.sha256
